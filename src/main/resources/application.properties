spring.application.name=Splitwise
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.show-sql=true
spring.jpa.show-sql=true
spring.datasource.url=jdbc:mysql://localhost:3306/splitwisedb
spring.datasource.username=root
spring.datasource.password=password

server.port=8082


#spring.jpa.hibernate.ddl-auto=create
# create : means it will delete all tables and data from db, and we have to add these data through initialize service as seed data
# update  : means that new data or changes will be done on top the data or table which is present in DB